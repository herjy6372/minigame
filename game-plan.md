# 미로 탈출 한 칸 모험 — 게임 기획서

## 1. 개발 방식과 제한 조건

- 화면 구조는 HTML, 디자인은 CSS, 게임 동작은 순수 JavaScript로 구현한다.
- PC와 모바일 웹 브라우저에서 실행하며 GitHub Pages로 배포한다.
- 서버, 데이터베이스, 회원가입, 로그인, 결제 기능은 사용하지 않는다.
- 설치가 필요한 외부 라이브러리는 사용하지 않는다.
- 외부 이미지 대신 이모지와 CSS 도형을 사용한다.
- 한 화면에서 게임 시작, 플레이, 결과 확인이 모두 이루어지게 한다.

## 2. 프로젝트 파일 및 폴더 구조

```text
maze-adventure/
├── index.html
├── style.css
├── game.js
├── assets/
│   ├── images/
│   └── sounds/
└── docs/
    ├── game-plan.md
    └── game-design.md
```

- `index.html`: 게임 화면 구조
- `style.css`: 색상, 크기, 배치, 반응형 디자인
- `game.js`: 미로, 이동, 충돌, 점수, 타이머, 게임 상태
- `assets/images/`: 추가 이미지가 필요할 때 사용
- `assets/sounds/`: 추가 효과음이 필요할 때 사용
- `docs/game-plan.md`: 게임 기획서
- `docs/game-design.md`: 게임 설계서

## 3. 화면 영역별 구성 요소

| 영역 | 구성 요소 | 역할 |
|---|---|---|
| 헤더 | 게임 제목, 한 줄 안내 | 게임의 목적과 방법 안내 |
| 정보 영역 | 현재 점수, 남은 시간 | 게임 진행 상태 표시 |
| 게임 영역 | 미로, 캐릭터, 출구 | 핵심 플레이 진행 |
| 조작 영역 | 방향 버튼 | 모바일 및 마우스 이동 조작 |
| 제어 영역 | 시작, 다시 하기 버튼 | 게임 시작과 재시작 |
| 결과 영역 | 성공·실패 문구, 최종 점수 | 게임 결과 안내 |

## 4. READY, PLAYING, GAME_OVER 게임 상태

### READY

- 게임 시작 전 상태이다.
- 점수는 0점, 남은 시간은 30초로 표시한다.
- 캐릭터를 출발 위치에 표시하고 시작 버튼을 활성화한다.
- 방향키와 방향 버튼으로 캐릭터를 움직일 수 없다.

### PLAYING

- 게임이 진행 중인 상태이다.
- 방향키 또는 화면 방향 버튼으로 캐릭터를 한 칸씩 이동한다.
- 남은 시간이 1초마다 감소한다.
- 이동할 때마다 벽 충돌과 출구 도착 여부를 확인한다.

### GAME_OVER

- 출구에 도착하거나 남은 시간이 0초가 되어 게임이 끝난 상태이다.
- 타이머와 캐릭터 이동을 중지한다.
- 성공 또는 실패 문구, 최종 점수, 다시 하기 버튼을 표시한다.

## 5. 저장해야 할 데이터

| 이름 | 초기값 | 역할 |
|---|---:|---|
| `score` | 0 | 현재 또는 최종 점수 |
| `timeLeft` | 30 | 남은 시간 |
| `gameState` | READY | 현재 게임 상태 |
| `playerRow` | 출발 행 | 캐릭터의 세로 위치 |
| `playerCol` | 출발 열 | 캐릭터의 가로 위치 |
| `mazeMap` | 고정 미로 배열 | 벽과 이동 경로 정보 |
| `exitPosition` | 출구 좌표 | 출구 도착 여부 확인 |
| `timerId` | null | 실행 중인 타이머 관리 |
| `resultType` | null | 성공 또는 실패 결과 |

모든 데이터는 브라우저의 JavaScript 변수에만 저장하며 페이지를 새로고침하면 초기화한다.

## 6. 사용자 입력과 처리 결과

| 사용자 입력 | 실행 조건 | 처리 내용 | 화면 결과 |
|---|---|---|---|
| 시작 버튼 클릭 | READY | 데이터를 초기화하고 타이머 시작 | 미로 활성화 및 시간 감소 |
| 키보드 방향키 입력 | PLAYING | 이동 가능 여부 확인 후 한 칸 이동 | 캐릭터 위치 갱신 |
| 화면 방향 버튼 터치 | PLAYING | 이동 가능 여부 확인 후 한 칸 이동 | 캐릭터 위치 갱신 |
| 벽 방향으로 이동 | PLAYING | 이동 취소 | 현재 위치 유지 |
| 출구 칸으로 이동 | PLAYING | 점수 계산 후 성공 처리 | 성공 문구와 최종 점수 표시 |
| 남은 시간이 0초가 됨 | PLAYING | 실패 처리 후 게임 종료 | 실패 문구와 0점 표시 |
| 다시 하기 클릭 | GAME_OVER | 모든 값을 초기화하고 재시작 | 새 게임 화면 표시 |

## 7. 필요한 기능 목록과 각 기능의 역할

- `startGame()`: 점수, 시간, 캐릭터 위치를 초기화하고 게임을 시작한다.
- `createMaze()`: 미로 배열을 읽어 벽과 길을 화면에 표시한다.
- `movePlayer()`: 입력한 방향으로 캐릭터 이동을 시도한다.
- `canMove()`: 다음 위치가 미로 안의 길인지 확인한다.
- `renderPlayer()`: 변경된 캐릭터 위치를 화면에 표시한다.
- `updateTimer()`: 남은 시간을 1초마다 줄이고 화면을 갱신한다.
- `checkExit()`: 캐릭터와 출구의 위치가 같은지 확인한다.
- `calculateScore()`: `100 + (남은 시간 × 10)`으로 성공 점수를 계산한다.
- `endGame()`: 타이머를 멈추고 성공·실패 결과를 표시한다.
- `resetGame()`: 모든 값을 초기화하고 다시 플레이할 수 있게 한다.

## 8. 오류를 예방하기 위한 예외 상황

- 시작 버튼을 여러 번 눌러도 타이머가 중복 실행되지 않아야 한다.
- READY 또는 GAME_OVER 상태에서는 캐릭터가 움직이지 않아야 한다.
- 캐릭터가 벽이나 미로 영역 밖으로 이동하지 않아야 한다.
- 남은 시간이 0보다 작아지지 않아야 한다.
- 출구 도착과 시간 초과가 동시에 중복 처리되지 않아야 한다.
- 게임 종료 후 입력해도 점수와 결과가 변경되지 않아야 한다.
- 다시 하기를 반복해도 점수, 시간, 위치가 정상적으로 초기화되어야 한다.
- 방향키를 눌렀을 때 웹페이지가 함께 스크롤되지 않아야 한다.

## 9. PC와 모바일 반응형 조건

- PC와 모바일에서 한 화면 안에 주요 기능을 표시한다.
- 게임 화면은 최대 너비를 지정하고 화면 가운데에 배치한다.
- 미로는 정사각형 비율을 유지하며 화면 너비를 넘지 않게 한다.
- 모바일 방향 버튼은 손가락으로 누르기 쉬운 크기로 만든다.
- 점수, 시간, 결과 문구는 작은 화면에서도 읽을 수 있어야 한다.
- PC에서는 키보드와 마우스, 모바일에서는 터치로 플레이할 수 있어야 한다.
- 작은 화면에서는 글자, 여백, 미로 칸 크기를 자동으로 줄인다.

## 10. 기능 구현 순서

1. HTML 화면 구조 작성
2. CSS 기본 배치 적용
3. 미로 데이터와 게임 상태 생성
4. 미로와 캐릭터 표시 기능 구현
5. 키보드 및 화면 버튼 이동 기능 구현
6. 벽과 미로 경계 충돌 판정 구현
7. 타이머와 시간 초과 기능 구현
8. 출구 도착과 점수 계산 기능 구현
9. 게임 종료와 다시 하기 구현
10. PC와 모바일 반응형 화면 적용
11. GitHub Pages 환경에서 전체 기능 테스트

## 11. 기능별 완료 조건

- [ ] 시작 버튼을 누르면 점수 0점, 남은 시간 30초로 게임이 시작된다.
- [ ] 플레이 중에만 캐릭터를 이동할 수 있다.
- [ ] 키보드와 화면 방향 버튼으로 캐릭터가 한 칸씩 이동한다.
- [ ] 캐릭터가 벽과 미로 경계를 통과하지 않는다.
- [ ] 타이머가 1초마다 정확하게 감소하고 0에서 멈춘다.
- [ ] 출구 도착 시 점수가 기획 규칙에 맞게 계산된다.
- [ ] 성공 또는 실패 시 게임이 종료되고 최종 결과가 표시된다.
- [ ] 다시 하기가 모든 값을 초기화하고 새 게임을 시작한다.
- [ ] PC와 모바일에서 주요 기능을 사용할 수 있다.
- [ ] GitHub Pages에서 별도 설치 없이 실행된다.
